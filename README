This program performs a Monte Carlo simulation to calculate pi. The program uses either single or double precision datatypes. The usage is:

./5A <digits of precision> <-s> (for single precision, double precision is default)
